# ===========================================
# VARIABLES DE ENTORNO PARA DOCKER COMPOSE
# Sistema ATS - SRI Ecuador
# ===========================================

# Copiar este archivo a .env y modificar los valores

# ===== CONFIGURACIÓN DE BASE DE DATOS =====
MYSQL_ROOT_PASSWORD=SuperRootPassword123!
DB_NAME=sri_ats
DB_USER=sriapp
DB_PASSWORD=SRIAppPassword123!
DB_PORT=3306

# ===== CONFIGURACIÓN DEL BACKEND =====
NODE_ENV=production
BACKEND_PORT=3000

# JWT - IMPORTANTE: Generar clave única y segura
# Usar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CAMBIAR_ESTA_CLAVE_POR_UNA_GENERADA_ALEATORIAMENTE_DE_64_CARACTERES
JWT_EXPIRE=8h
JWT_REFRESH_EXPIRE=7d

# CORS - Cambiar a tu dominio real en producción
CORS_ORIGIN=http://localhost

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# ===== CONFIGURACIÓN DEL FRONTEND =====
FRONTEND_PORT=80
VITE_API_URL=/api
VITE_APP_NAME=Sistema ATS - SRI Ecuador

# ===== SERVICIOS OPCIONALES =====
# phpMyAdmin (solo desarrollo)
PHPMYADMIN_PORT=8081
